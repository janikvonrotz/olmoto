const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../events';

describe('event.actions.events', () => {
  it('should do something');
});
